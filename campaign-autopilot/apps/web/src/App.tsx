import { useState } from 'react'
import { campaigns, issues, type Issue } from './data'
import { DotField, LogoCloud, Mark } from './brand'

type Page = 'Overview' | 'Issues' | 'Approvals' | 'Campaigns' | 'Experiments' | 'Ask the data' | 'Settings'
const nav: Page[] = ['Overview', 'Issues', 'Approvals', 'Campaigns', 'Experiments', 'Ask the data', 'Settings']
const Icon = ({ name }: { name: string }) => <span className="nav-icon" aria-hidden="true">{name}</span>

function Status({ children }: { children: string }) { return <span className={`status ${children.toLowerCase().replaceAll(' ', '-')}`}>{children}</span> }
function IssueCard({ issue, select }: { issue: Issue; select: (issue: Issue) => void }) { return <button className="issue-card" onClick={() => select(issue)}><div className="issue-top"><Status>{issue.severity}</Status><span className="muted">{issue.channel} · {issue.status}</span></div><h3>{issue.title}</h3><p>{issue.summary}</p><div className="issue-bottom"><span>{issue.campaign}</span><strong>{issue.confidence}% confident →</strong></div></button> }

function Overview({ select }: { select: (issue: Issue) => void }) { return <><section className="page-heading"><div><p className="eyebrow">BREW & BLOOM · LAST 30 DAYS</p><h1>Good afternoon, Mel.</h1><p className="subtitle">Here’s what needs your attention today.</p></div><button className="date-button">Sep 1 – Sep 30⌄</button></section><section className="metrics"><Metric name="Shopify revenue" value="$40,280" detail="↑ 8.4% vs. prior period" /><Metric name="Ad spend" value="$19,480" detail="↑ 4.1% vs. prior period" /><Metric name="Blended ROAS" value="3.7×" detail="↑ 0.2× vs. prior period" /><Metric name="Needs attention" value="3" detail="1 needs approval" alert /></section><section className="split"><div className="panel"><div className="panel-title"><div><h2>Needs attention</h2><p>Prioritized by impact and urgency</p></div><button className="text-button">View all issues</button></div><div className="issue-list">{issues.slice(0, 3).map(i => <IssueCard key={i.id} issue={i} select={select} />)}</div></div><div className="panel activity"><div className="panel-title"><div><h2>Agent activity</h2><p>Transparent, source-backed decisions</p></div></div><Timeline /></div></section><section className="panel campaign-panel"><div className="panel-title"><div><h2>Campaign performance</h2><p>Compared with your rolling baseline</p></div><button className="text-button">View campaigns</button></div><CampaignTable /></section></> }
function Metric({ name, value, detail, alert }: { name: string; value: string; detail: string; alert?: boolean }) { return <div className="metric"><p>{name}</p><strong>{value}</strong><span className={alert ? 'alert-text' : 'positive'}>{detail}</span></div> }
function Timeline() { return <ol className="timeline"><li><span className="dot warning"/><div><strong>Approval requested</strong><p>Refresh Meta Prospecting creative</p><small>12 minutes ago</small></div></li><li><span className="dot"/><div><strong>Issue diagnosed</strong><p>Creative fatigue detected from metric evidence</p><small>18 minutes ago</small></div></li><li><span className="dot"/><div><strong>Data synchronized</strong><p>Shopify, Meta, Google, and Klaviyo are current</p><small>42 minutes ago</small></div></li></ol> }
function CampaignTable() { return <div className="table-wrap"><table><thead><tr><th>Campaign</th><th>Channel</th><th>Spend</th><th>ROAS</th><th>Trend</th><th>Status</th></tr></thead><tbody>{campaigns.map(row => <tr key={row[0]}>{row.map((item, i) => <td key={i}>{i === 4 ? <span className={item.startsWith('+') ? 'positive' : item.startsWith('-') ? 'negative' : ''}>{item}</span> : i === 5 ? <Status>{item}</Status> : item}</td>)}</tr>)}</tbody></table></div> }

function Issues({ select }: { select: (issue: Issue) => void }) { return <><section className="page-heading"><div><p className="eyebrow">WORK QUEUE</p><h1>Issues & opportunities</h1><p className="subtitle">Every recommendation is traceable to the metrics behind it.</p></div></section><div className="filters"><button className="filter active">All · 3</button><button className="filter">Needs approval · 1</button><button className="filter">High severity · 1</button><button className="filter">Meta⌄</button></div><div className="issues-grid">{issues.map(i => <IssueCard key={i.id} issue={i} select={select} />)}</div></> }
function Detail({ issue, back, approve }: { issue: Issue; back: () => void; approve: () => void }) { return <><button className="back" onClick={back}>← Back to issues</button><section className="detail-heading"><div><div className="inline"><Status>{issue.severity}</Status><span className="muted">{issue.status}</span></div><h1>{issue.title}</h1><p className="subtitle">{issue.campaign} · {issue.channel} · {issue.confidence}% confidence</p></div><button className="primary" onClick={approve}>Review recommendation</button></section><div className="detail-grid"><div><section className="panel"><h2>What happened</h2><p className="body-copy">{issue.summary} The system compared current results with a rolling 30-day baseline and found a sustained change, not a one-day anomaly.</p></section><section className="panel"><h2>Metric evidence</h2><p className="muted">Source: Meta Ads · Sep 1–30</p><div className="evidence">{issue.evidence.map(x => <div key={x}><span className="chart-line"/><strong>{x}</strong></div>)}</div><div className="fake-chart"><span>Sep 1</span><svg viewBox="0 0 400 100" preserveAspectRatio="none"><polyline points="0,20 55,28 110,24 165,44 220,51 275,67 330,65 400,88" fill="none" stroke="currentColor" strokeWidth="3"/></svg><span>Today</span></div></section></div><aside><section className="panel recommendation"><p className="eyebrow">RECOMMENDED ACTION</p><h2>Refresh prospecting creative</h2><p>Launch two new creative variants and pause the fatigued asset after approval.</p><hr/><div><span>Risk tier</span><strong>High · approval required</strong></div><div><span>Expected impact</span><strong>$1,240 monthly recovery</strong></div><div><span>Guardrail</span><strong>No budget increase</strong></div><button className="primary full" onClick={approve}>Review & approve</button></section></aside></div></> }
function Approvals({ approve }: { approve: () => void }) { const [done, setDone] = useState(false); return <><section className="page-heading"><div><p className="eyebrow">HUMAN REVIEW REQUIRED</p><h1>Approvals</h1><p className="subtitle">High-impact changes stay in your control.</p></div></section>{done ? <div className="success"><h2>Creative refresh approved</h2><p>The recommendation is now queued. The dashboard will measure the result when new data arrives.</p></div> : <section className="approval panel"><div><Status>High</Status><h2>Refresh Meta Prospecting creative</h2><p>Creative fatigue is causing reduced efficiency. This action does not change the campaign budget.</p><div className="comparison"><div><span>Current</span><strong>CTR 1.08%</strong><small>Frequency 3.8</small></div><div><span>Expected after refresh</span><strong>CTR toward 1.72%</strong><small>Two new variants</small></div></div><div className="actions"><button className="secondary">Reject</button><button className="secondary">Edit guardrail</button><button className="primary" onClick={() => { setDone(true); approve() }}>Approve action</button></div></div><aside className="audit"><h3>Why you can trust this</h3><p>92% confidence based on 30 days of Meta Ads data.</p><p>No spend cap or targeting changes are included.</p><button className="text-button">View audit evidence →</button></aside></section>}</> }
function Placeholder({ page }: { page: Page }) { return <section className="placeholder"><p className="eyebrow">CAMPAIGN AUTOPILOT</p><h1>{page}</h1><p className="subtitle">This UI area is ready for the next frontend build step.</p></section> }

function Landing({ launch }: { launch: () => void }) { return <div className="landing"><header className="landing-header"><button className="journey-brand" onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })}><Mark size={26} /> Journey Edge</button><nav><a href="#product">Product</a><a href="#workflow">How it works</a><a href="#control">Safety</a><button className="sign-in" onClick={launch}>Sign in</button><button className="primary" onClick={launch}>See the demo <b>→</b></button></nav></header><main><section className="hero"><div className="hero-copy"><p className="landing-eyebrow">AI MARKETING OPERATIONS FOR SHOPIFY BRANDS</p><h1>Know what to fix.<br/><em>Understand why.</em><br/>Stay in control.</h1><p>Journey Edge turns marketing and Shopify data into evidence-backed recommendations, then keeps your team in charge of high-impact decisions.</p><div className="hero-actions"><button className="primary large" onClick={launch}>See the live demo <b>→</b></button><a href="#workflow" className="watch-link">Explore the workflow <span>↓</span></a></div><div className="source-row"><span><i>✓</i> Shopify revenue truth</span><span><i>✓</i> Approval-based actions</span></div></div><DashboardPreview launch={launch}/></section><LogoCloud/><section className="proof" id="product"><p className="landing-eyebrow">FROM SIGNAL TO SAFE ACTION</p><h2>Your marketing data should lead<br/>to <em>decisions.</em></h2><div className="proof-grid"><Proof icon="⌁" title="Detect" text="Spot campaign issues and growth opportunities before performance drifts further."/><Proof icon="⌘" title="Diagnose" text="See the metrics and baselines behind every recommendation."/><Proof icon="✓" title="Act safely" text="Use guardrails and human approval for high-impact changes."/></div></section><section className="workflow" id="workflow"><div className="workflow-heading"><p className="landing-eyebrow">A CALMER WAY TO OPERATE</p><h2>From scattered data<br/>to <em>clear next steps.</em></h2><p>Journey Edge gives your team a connected decision loop—not another dashboard to check.</p></div><div className="steps"><Step number="01" title="Connect your data" text="Bring Shopify, paid media, and email performance into one view."/><Step number="02" title="Find what changed" text="Spot issues and opportunities against your normal baseline."/><Step number="03" title="See the evidence" text="Understand what happened, why it matters, and how certain we are."/><Step number="04" title="Review the action" text="Approve, reject, or adjust high-impact recommendations."/><Step number="05" title="Measure what worked" text="Track outcomes so every future decision improves."/></div></section><section className="control" id="control"><div><p className="landing-eyebrow">AUTOMATION WITH BOUNDARIES</p><h2>Useful AI.<br/><em>Your rules.</em></h2><p>Journey Edge is built to make recommendations understandable and actions accountable.</p></div><div className="control-list"><Control title="No invented numbers" text="Every narrative is tied to real data, timeframes, and sources."/><Control title="No unapproved high-risk changes" text="Spend caps, confidence thresholds, and approval rules remain with your team."/><Control title="A decision trail you can trust" text="Rationale, confidence, approvals, and outcomes live in one audit history."/></div></section><section className="story"><div className="story-label"><p className="landing-eyebrow">A DEMO SCENARIO</p><h2>Meet Brew<br/>& Bloom.</h2><p>A coffee brand seeing its strongest prospecting creative lose efficiency.</p><span>Simulated demo data</span></div><div className="story-flow"><StoryCard step="The signal" title="Creative fatigue detected" body="CTR fell 37% while ad frequency climbed above its normal range."/><div className="flow-arrow">↓</div><StoryCard step="The recommendation" title="Refresh creative, hold budget" body="Two new variants are ready. No spend increase is proposed."/><div className="flow-arrow">↓</div><StoryCard step="The decision" title="You stay in control" body="Review the evidence and approve only when the action is right."/></div></section><section className="closing"><DotField area="closing"/><p className="landing-eyebrow">MAKE YOUR NEXT MOVE A SMARTER ONE</p><h2>Stop hunting for answers<br/>across marketing dashboards.</h2><p>Give your team a clearer, safer way to act on campaign performance.</p><button className="primary large" onClick={launch}>Explore Journey Edge <b>→</b></button></section></main><footer><DotField area="footer"/><div className="journey-brand"><Mark size={24} /> Journey Edge</div><p>Evidence-led marketing operations.</p><div><a href="#product">Product</a><a href="#workflow">Workflow</a><a href="#control">Safety</a></div></footer></div> }
function Proof({ icon, title, text }: { icon: string; title: string; text: string }) { return <article><span className="proof-icon">{icon}</span><h3>{title}</h3><p>{text}</p></article> }
function Step({ number, title, text }: { number: string; title: string; text: string }) { return <article><span>{number}</span><h3>{title}</h3><p>{text}</p></article> }
function Control({ title, text }: { title: string; text: string }) { return <article><span>✓</span><div><h3>{title}</h3><p>{text}</p></div></article> }
function StoryCard({ step, title, body }: { step: string; title: string; body: string }) { return <article><span>{step}</span><h3>{title}</h3><p>{body}</p></article> }
function DashboardPreview({ launch }: { launch: () => void }) { return <div className="preview-wrap"><DotField area="hero"/><div className="preview-glow"/><div className="preview"><div className="preview-bar"><b><Mark size={13} />Journey Edge</b><span>Brew & Bloom</span><i>MB</i></div><div className="preview-body"><div className="preview-side"><span className="active">Overview</span><span>Issues <b>1</b></span><span>Approvals</span><span>Campaigns</span></div><div className="preview-content"><p>GOOD AFTERNOON, MEL</p><h3>Here’s what needs your attention.</h3><div className="mini-metrics"><div><span>Revenue</span><b>$40,280</b><small>↑ 8.4%</small></div><div><span>Blended ROAS</span><b>3.7×</b><small>↑ 0.2×</small></div></div><button className="preview-issue" onClick={launch}><div><label>HIGH PRIORITY</label><strong>Creative fatigue is reducing prospecting efficiency</strong><span>Frequency ↑ · CTR ↓ · 92% confidence</span></div><b>→</b></button></div></div></div><div className="floating-note"><span>✓</span><div><b>Evidence, not guesswork</b><small>Every recommendation is traceable.</small></div></div></div> }

export default function App() { const [landing, setLanding] = useState(true); const [page, setPage] = useState<Page>('Overview'); const [detail, setDetail] = useState<Issue | null>(null); const [notice, setNotice] = useState(''); const select = (issue: Issue) => { setDetail(issue); setPage('Issues') }; const goApproval = () => { setDetail(null); setPage('Approvals') }; if (landing) return <Landing launch={() => setLanding(false)} />; return <div className="app"><aside className="sidebar"><div className="brand"><Mark size={30} /><div>Journey<br/><b>Edge</b></div></div><nav>{nav.map(item => <button key={item} onClick={() => { setPage(item); setDetail(null) }} className={page === item && !detail ? 'selected' : ''}><Icon name={item === 'Overview' ? '⌂' : item === 'Issues' ? '◉' : item === 'Approvals' ? '✓' : item === 'Campaigns' ? '▦' : item === 'Experiments' ? '◫' : item === 'Ask the data' ? '✦' : '⚙'} />{item}{item === 'Approvals' && <span className="count">1</span>}</button>)}</nav><div className="sidebar-bottom"><div className="sync"><span className="live"/> Data current<br/><small>Updated 12 min ago</small></div><button className="account"><span>MB</span> Mel Brooks <b>⌄</b></button></div></aside><main><header className="topbar"><div className="crumb">Brew & Bloom <span> / </span> Marketing workspace</div><div><button className="help">?</button><button className="avatar">MB</button></div></header><div className="content">{notice && <div className="toast">{notice}</div>}{detail ? <Detail issue={detail} back={() => setDetail(null)} approve={goApproval} /> : page === 'Overview' ? <Overview select={select} /> : page === 'Issues' ? <Issues select={select} /> : page === 'Approvals' ? <Approvals approve={() => setNotice('Approval recorded in the local demo.')} /> : page === 'Campaigns' ? <><section className="page-heading"><div><p className="eyebrow">PERFORMANCE</p><h1>Campaigns</h1><p className="subtitle">Performance compared with your rolling baseline.</p></div></section><section className="panel campaign-panel"><CampaignTable /></section></> : <Placeholder page={page} />}</div></main></div> }
